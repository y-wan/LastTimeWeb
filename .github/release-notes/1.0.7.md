Fix Microsoft reconnection in the iPhone Home Screen app by using page redirect instead of a blocked popup. When background sync needs renewed authorization, the app now waits for an explicit reconnect rather than attempting to open a popup.

No data migration is required. Reconnect to Microsoft from the installed app to resume OneDrive sync.
